/** every dimension is on rem */

const dimensions = {}

const colors = {
  main: '#fffa34',
}

const breakpoints = {
  sm: '576px',
  md: '768px',
  lg: '992px',
  xl: '1200px',
}

export const defaultTheme = {
  colors,
  dimensions,
  breakpoints,
  fonts: ['Roboto'],
}
